from flask import Blueprint

# This file might not be needed if we use app/routes/
# But the prompt structure had 'routes/' at root too.
# I will stick to app/routes as it is cleaner and I used it in __init__.py
